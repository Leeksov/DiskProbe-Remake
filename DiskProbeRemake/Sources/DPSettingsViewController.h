#import <UIKit/UIKit.h>

// DPSettingsViewController is a PSListController (Preferences.framework)
// For rootless standalone app, we reimplement it as a UITableViewController
@interface DPSettingsViewController : UITableViewController
@end
